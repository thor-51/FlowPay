spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.example.transactionprocessing: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

app:
  seed:
    enabled: true
    admin-email: admin@tps.local
    admin-password: Admin@12345  # dev-only seed user, never used in prod
